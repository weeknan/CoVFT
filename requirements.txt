# CoVFT - Core Dependencies
# Python >= 3.8 recommended, 3.10 tested

# Deep Learning
torch>=2.0.0
transformers>=4.31.0
accelerate>=0.20.0

# Distributed Training
deepspeed>=0.11.0

# Parameter-Efficient Fine-tuning (LoRA baseline)
peft>=0.4.0

# Vision & Tokenizer
Pillow>=9.0.0
sentencepiece>=0.1.99

# Training Logging
wandb

# Evaluation
pandas
shortuuid
tqdm

# Utilities
packaging

# Optional: Flash Attention 2 (recommended for faster training)
# pip install flash-attn --no-build-isolation
# flash-attn>=2.0.0

# Optional: xformers (alternative to flash-attn if CUDA compatible)
# xformers
